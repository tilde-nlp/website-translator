export interface IInternalUiOptions {
    ui: {
        branding: {
          icon: string
        },
        translationProvider: string,
        icons: {
          menuIcon: string,
        }
    }
}
